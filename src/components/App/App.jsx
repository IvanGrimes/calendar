import React, { Fragment } from 'react';
import Calendar from '../Calendar';
import Events from '../Events';

const App = () => (
  <Fragment>
    <Calendar />
    <Events />
  </Fragment>
);

export default App;
